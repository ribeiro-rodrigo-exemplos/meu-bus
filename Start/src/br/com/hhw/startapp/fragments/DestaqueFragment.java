package br.com.hhw.startapp.fragments;import com.nostra13.universalimageloader.core.ImageLoader;import br.com.hhw.startapp.R;import br.com.hhw.startapp.handlers.DestaqueModelHandler;import br.com.hhw.startapp.helpers.ServiceRestClientHelper;import br.com.hhw.startapp.models.DestaqueModel;import br.com.hhw.startapp.models.ws.StartAppServices;import android.app.Activity;import android.os.Bundle;import android.support.v4.app.Fragment;import android.view.LayoutInflater;import android.view.MenuItem;import android.view.View;import android.view.ViewGroup;import android.view.ViewStub;import android.widget.ImageView;import android.widget.ProgressBar;import android.widget.ScrollView;import android.widget.TextView;public class DestaqueFragment extends Fragment {		private Activity ownerActivity;		private ProgressBar spinner;	private DestaqueModel destaque;	private ImageView imageView;	private TextView nome;	private TextView descricao;	private ViewStub internetFailureViewStub;	private ScrollView scrollView;	@Override	public View onCreateView(LayoutInflater inflater, ViewGroup container,			Bundle savedInstanceState) {		setHasOptionsMenu(true);				View contentView = inflater.inflate(R.layout.fragment_destaque, container,				false);		getViews(contentView);		loadDestaque();		return contentView;	}	@Override	public void onAttach(Activity activity) {		super.onAttach(activity);		ownerActivity = activity;	}		@Override	public boolean onOptionsItemSelected(MenuItem item) {//		int id = item.getItemId();//		if (id == R.id.action_share) {//			new ShareHelper(ownerActivity)//					.sharePrintScreenFromView(ownerActivity.getWindow()//							.getDecorView().getRootView());//		}		return true;	}	@Override	public void onDestroy() {		super.onDestroy();		ServiceRestClientHelper.cancelRequests(ownerActivity);	}		private void getViews(View contentView) {		spinner = (ProgressBar) contentView.findViewById(R.id.pacote_progressBar);		imageView = (ImageView) contentView.findViewById(R.id.pacote_imageview);		nome = (TextView) contentView.findViewById(R.id.pacote_textview_nome);		descricao = (TextView) contentView.findViewById(R.id.pacote_textview_descricao);		scrollView = (ScrollView) contentView.findViewById(R.id.pacote_scrollView);	}		private void loadDestaque() {		spinner.setVisibility(View.VISIBLE);		StartAppServices.getDestaque(1, new DestaqueModelHandler() {			@Override			public void setDestaque(DestaqueModel destaqueFromHandler) {				destaque = destaqueFromHandler;				showDestaque();			}			@Override			public void setErro(Throwable e) {//				showError();			}		});	}		private void showDestaque() {		spinner.setVisibility(View.GONE);//		internetFailureViewStub.setVisibility(View.GONE);		scrollView.setVisibility(View.VISIBLE);		nome.setText(destaque.nome);		descricao.setText(destaque.descricao);		ImageLoader.getInstance().displayImage(destaque.urlImagem, imageView);	}}